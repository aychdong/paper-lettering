# 纸上文字 0.6

双击 photo-studio 中「纸上文字.app」；在独立浏览器里编辑，关闭 ChatGPT 界面仍可使用。编辑器核心是离线 HTML/Canvas，Mac .app 是启动入口。AI 助手额外需要本机 Codex 运行组件、现有 ChatGPT 登录和网络；没有这些也能完成全部手工排版与导出。

## 别人如何使用

分享 `Paper-Lettering-0.6.zip`，解压后用近期 Chrome / Edge 打开「纸上文字.html」，内置「追着光跑」「并肩看远方」两张可编辑示例，均已预置真实 AI 排版与配色，无需安装字体或服务器。先试改示例文字，或点顶部「打开图片」。本地分享包仅加入用户授权的两张生成图，不含原片、个人草稿、账号或凭据；本轮未公开发布。

HTML 核心可用于 macOS、Windows、Linux。当前已在 macOS Chrome 实测，Windows/Linux 未做实机验证。本机 .app 为 Apple Silicon 启动器，不是 Windows 安装包，也还不是经过商店签名和公证的发行版。跨平台先使用 HTML 包。

可选 AI：安装 Python 3.9+、官方 Codex CLI，双击「连接自己的ChatGPT」或运行 `codex login` 选择 ChatGPT；再启动包内对应系统的「启用AI助手」脚本。每人使用自己的账号，本版只有 Codex / ChatGPT 适配器，没有通用第三方 API 地址或密钥设置。也可在包目录运行 `python3 ai/bridge.py --html 纸上文字.html --open`。无需输入 API Key；不复用网页 cookie。服务只监听本机 127.0.0.1。编辑器打开期间自动维持连接；关闭后闲置 15 分钟退出。App 启动后自动检查现有登录，无需手动交换文件或复制连接地址。

## 从打开到导出

顶部按顺序提供「打开图片 → 打开可编辑工程 → 保存可编辑工程 → 导出成品 PNG」。打开图片会添加底图，左下的加号仍保留。打开工程用于继续编辑以前保存的文字层。

0.6 首次使用直接显示上述两张示例，打开时已经应用首套 AI 推荐。点「查看方案」还能离线查看、应用另一套；颜色页也保留 AI 色卡与理由。本轮不自动迁入旧版四张图的草稿，旧版 App 和草稿键仍保留，可以另存工程再手工打开。左侧「添加两张示例」可随时找回示例，只补充缺少的图片，不替换已有修改。

App 打开后，看画布上方的状态：旋转圆点表示正在连接，绿色「AI 已连接 · ChatGPT」表示可以使用。橙色状态可以点击查看原因或重试。直接打开 HTML 是离线入口；需要 AI 时双击 App，或便携包的「启用AI助手」，使用它新打开的页面。旧的 0.4 标签页不会自动升级，保存工程后可关闭。

点击排版后会显示动态进度条、当前阶段和已等待时间，完成或失败都会给出明确提示。进度条表示正在工作，不是模型完成百分比。通常需要等待一段时间；等待期间可以继续编辑，新的修改会受到保护。自动连接本身只检查登录，只有点击 AI 请求才发送预览。

## 先一键初排，再用四页微调

画布上方的「一键 AI 排版」是主入口：已有图片且已连接时，自动获取方案并应用第一套，然后打开文字页，直接微调。可点「查看方案」换另一套，也可 ⌘/Ctrl Z 撤销。没有图片时该位置显示「导入图片开始」；没有连接时会带你到 AI 页说明如何连接。

右侧固定四个带平面线条图标的标签页：

| 标签 | 内容 |
| --- | --- |
| 文字 | 文案、字体、字号、字重、增粗；字距/方向/位置、倾斜/描边和扩展字库收在折叠区 |
| 颜色 | 取色、色号、色卡、本地配色和 AI 配色 |
| 质感 | 九种纸面效果、强度、浓淡、颗粒、磨损和套印副色 |
| AI 排版 | 补充要求、连接状态、先生成两套再选择应用 |

图层选择、添加、锁定和隐藏固定在标签下方，跨页可用；更多导出固定在底部。各页记住滚动位置，标签记住上次选择。键盘聚焦标签后，可用左右方向键、Home 和 End 切换。

一键初排只在等待期间文字和底图覆盖参数保持不变、仍停在同张照片时自动应用。你若继续改字、锁定图层或切换照片，返回方案只会保存供你选择，不覆盖新修改。请求失败保持工程不变，可以重试。

## 直接操作

- 拖文字移动，拖四角缩放字号，拖圆柄旋转；Shift 吸附 15°。
- 「预览成图 · P」或输入框外按 P 隐藏辅助框。导出 PNG 从不带框。
- 原生「字重」用于支持的可变字体；固定字体用「笔画增粗」，不会放大整行。加厚过多会堵塞小字的内部空间。
- 「倾斜」是几何倾斜，适用于中文、手写等；不等同于专门设计的英文 Italic 字形。
- 描边可选择宽度和颜色。字距、行距、横/竖排、段落对齐及画布六方向对齐均可调。
- 图层列表从上到下对应画面的叠放顺序。拖动一行或左侧点阵柄排序；插入线标明位置。键盘聚焦图层后也可按 Alt + 上/下方向键排序。
- 下方只有「锁定这层」和「隐藏这层」。锁定后不能编辑、拖动或删除，先解锁才能应用整套 AI 排版。隐藏保留参数，再点「显示这层」恢复。
- 每行右侧垃圾桶直接删除该层，可用 ⌘/Ctrl Z 撤销；不需要先选中该层。
- 方向键移动 1 像素，Shift + 方向键 10 像素；⌘/Ctrl Z 撤销，Shift Z 重做，⌘/Ctrl S 保存工程。

内置 6 款中文、6 款英文字体，原始 OFL 文件离线保存。支持的浏览器可授权读取系统字体列表；只引用名称，不复制 Apple 字体。也可导入有权使用的 TTF/OTF/WOFF 文件。可变字重范围直接读取字体自身的 fvar 表。

## 九种纸面质感

| 质感 | 可见变化 |
| --- | --- |
| 纸上油墨 | 大片浓淡起伏、纸纤维缺墨 |
| 褪色铅字 | 降低墨量，断续的旧字边缘 |
| 旧式印章 | 孔洞、磨损，可添加破损外框 |
| 清晰印字 | 完整字形，适合小字号 |
| 凹版压印 | 位移的亮暗边，模拟纸面压痕 |
| 丝网漏印 | 周期性网纹与漏墨 |
| 铅笔拓写 | 方向性排线、石墨深浅 |
| 宣纸洇墨 | 墨边扩散与柔化 |
| 双色套印 | 主副色错位，模拟两次套印 |

后五种的「质感强度」调整其专属特征；墨量、颗粒、起伏和边缘磨损继续可调。双色套印另有副色。它们是确定性的视觉模拟，不是印刷工艺的物理仿真。小字请先看实际成图，避免缺墨损害辨认。

## 一键 AI 建议

从 .app 或 AI 启动器打开后，右侧显示连接状态。按「让 AI 推荐配色」获得颜色及理由；切到「AI 排版」页，按「生成两套方案供我选」获得两套预览，可补充“字更厚、文案保留、用蓝绿色”等要求，再点击「应用此方案」。此页先展示建议；主界面「一键 AI 排版」会直接初排。两种方式应用后均可撤销。

点击请求时才发送：当前去元数据的 PNG 派生预览（最长边 1200）、当前文字与排版参数、补充要求、候选颜色。使用现有 ChatGPT/Codex 额度；原片路径、EXIF/GPS 和账号凭据不进入请求。预览由浏览器重新编码，即使导入图本来有 EXIF，也不会照搬上传。

AI 通过官方 Codex App Server 处理，并非嵌入 ChatGPT 网页。未登录时先在 Codex 登录，再检查连接；服务关闭或无网时继续手工编辑。每次建议会保留模型返回的标识、时间、预览哈希及中文理由；内部模型修订不可锁定，因此建议本身不保证重复一致。参数固定后的本地成图可重复。

本地配色继续根据 OKLCH 邻近/互补等关系运行，无需 AI 或联网。对比度参考只比较实色与取样，不包括缺墨、透明度和混合，不等于最终成图的无障碍认证。需要文件交接时，展开「离线文件交换」即可使用原来的导出/导入。

## 工程和图片不同

| 按钮 | 用途 |
| --- | --- |
| 打开图片 / 左下导入自己的图片 | 加入 PNG/JPEG/WebP 底图 |
| 保存可编辑工程 | 保存 `.paper.json`：底图、独立文字、字体、参数、AI 方案与理由 |
| 打开可编辑工程 | 继续修改上次保存的文字层 |
| 导出成品 PNG | 合并的最终图片，适合分享与打印 |

每次保留「工程 + 成品」。浏览器草稿不是备份；换浏览器、清缓存或来源改变可能读不到。0.6 使用新的示例草稿空间，旧版工程与草稿仍保留。保存工程仅嵌入实际用到的内置/导入字体和许可证；系统字体仅保存名称。

底图已有的像素字无法自动还原为原字体。附近纸纹覆盖仅适合空白纸面；主体上的旧字需单独处理。今后优先生成无字底图，再独立排字。

## 在工作流里调用

本机说「用 paper-lettering 打开编辑器」，或「用实景拼贴生成无字底图，再用 paper-lettering 自动决定中文文案、字体、配色和版式，给我可编辑工程和 PNG」。助手先完成初排，再交给你自由调整。详细自动阶段见 photo-studio 的 `docs/LETTERING-WORKFLOW.md`。

Skill 的可用性取决于当前客户端是否加载及能否访问本机文件；本机安装不会自动赋予普通网页 ChatGPT 操作电脑的能力。无本机访问时仍可提交配色请求文件给助手，但打开编辑器由你完成。浏览器下载不会自动登记入库；归档前将文件放回对应实验并更新输出清单。
