# 图片与账号数据

手工编辑、预置方案、内置字库与本地配色均可离线运行。项目不含遥测、CDN 字体、远程统计或共享账号。

点击 AI 请求时，浏览器将当前底图和可撤销纸面覆盖渲染为最长边不超过 1200 的 PNG；重新编码会去除原文件 EXIF/GPS。请求还包含当前文案、画面尺寸、白名单排版参数、用户填写的要求及图像哈希。不会发送原片路径或从原片复制元数据。

桥接只在本机回环地址运行；只有持有本次连接 capability 的页面能调用。Codex 使用你自己的本机官方登录，按所用账号和服务的政策处理 AI 请求。纸上文字不读取浏览器 cookie、不要求把账号令牌交给项目作者，也不在发行包携带凭据。

工程和自动草稿保存在本机：草稿在当前浏览器的 IndexedDB，导出文件由浏览器保存。临时 AI 预览在任务完成后删除；尚未退出的本机服务会暂存最多 20 项结果。完整方案和理由可保存在可编辑工程内。关闭页面后服务闲置 15 分钟退出。

问题报告请不要上传原片、完整工程、账号文件或带 token 的页面地址。先用内置示例复现，再提供去掉敏感内容的错误信息。
