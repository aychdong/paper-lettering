const {chromium}=require('playwright'),fs=require('fs'),path=require('path'),assert=require('assert/strict'),{pathToFileURL}=require('url');
const root=path.resolve(__dirname,'..'),out=process.argv[2]||'/tmp/paper-fixed-cases';fs.mkdirSync(out,{recursive:true});
(async()=>{const b=await chromium.launch({headless:true,executablePath:process.env.LETTERING_CHROME||'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage();await p.goto(pathToFileURL(path.join(root,'dist/Paper-Lettering-0.8.0-preview.1/纸上文字.html')).href);await p.waitForFunction(()=>paperStudio?.ready);await p.addScriptTag({path:path.join(root,'evaluations/fixtures.js')});const cases=JSON.parse(fs.readFileSync(path.join(root,'evaluations/cases.json'))).cases,checks=[];for(const c of cases){const v=await p.evaluate(async c=>{const {source:d}=await PaperEvaluationFixture(c);const brief={id:c.id,name:'固定排印测试',reason:'几何与字体检查，不评价文案',poetic:'yes',phrases:['ChatGPT','2026年'],layers:[{text:c.copyMode==='preserve'?c.text:'风经过，光留下。',font:'gf-notoserifsc',size:.035,weight:600,thickness:.01,slant:0,x:.6,y:.08,rotation:0,direction:c.seed%2?'horizontal':'vertical',align:'left',tracking:.04,lineHeight:1.5,color:'#342f27',effect:'ink'}]};const r=await PaperQuality.search([brief],{regions:[]},d,evaluationImage,{placement:c.placement});return {id:c.id,count:r.candidates.length,diagnostics:r.diagnostics,checks:r.candidates[0]?.checks};},c);assert.equal(v.count,1,JSON.stringify(v));assert.deepEqual(v.checks.errors,[]);checks.push(v);}fs.writeFileSync(path.join(out,'checks.json'),JSON.stringify({status:'passed',scope:'24 deterministic fixture trials; not human aesthetic evaluation',checks},null,2));console.log('24 deterministic fixture trials passed');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
